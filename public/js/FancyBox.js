$(document).ready(function() {
    //$("#carousel a, .picList a")
    //    .attr('rel', 'gallery')
    //    .fancybox({
    //        openEffect: 'none',
    //        closeEffect: 'none',
    //        margin: [20, 60, 20, 60]
    //    });

    $("#carousel a")
        .attr('rel', 'gallery')
        .fancybox({
            openEffect: 'none',
            closeEffect: 'none',
            margin: [20, 60, 20, 60]
        });

    $('#slide_fotos_estudio a')
        .attr('rel', 'gallery')
        .fancybox({
            openEffect: 'none',
            closeEffect: 'none',
            margin: [20, 60, 20, 60]
        });

    $('#slide_fotos_musicos a')
        .attr('rel', 'gallery')
        .fancybox({
            openEffect: 'none',
            closeEffect: 'none',
            margin: [20, 60, 20, 60]
        });        

    $('#slide_fotos_participacoes a')
        .attr('rel', 'gallery')
        .fancybox({
            openEffect: 'none',
            closeEffect: 'none',
            margin: [20, 60, 20, 60]
        });
});
